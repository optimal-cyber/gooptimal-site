---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import GatewayCanvas from "../components/GatewayCanvas.astro";
import VetBadge from "../components/VetBadge.astro";
import {
  MANAGED_STARTING_PRICE,
  BUILD_PROJECT_RANGE,
  G3_LIST_PRICE,
  G5_LIST_PRICE,
  ESP_POSTURE,
  RPO_STATUS,
  pending,
} from "../data/company.ts";

// Why a DIB company needs GCC High — the compliance drivers.
const drivers = [
  {
    tag: "CUI",
    title: "Controlled Unclassified Information",
    body: "CUI carries handling requirements your prime and the DoD will hold you to. GCC High is built to store and process it inside a US-sovereign boundary — not bolted onto commercial email.",
  },
  {
    tag: "ITAR / EAR",
    title: "Export-controlled technical data",
    body: "Export-controlled data must stay with US persons on US soil. GCC High provides screened US-person support and US data residency — the access control ITAR and EAR expect.",
  },
  {
    tag: "DFARS 7012",
    title: "The clause already in your contracts",
    body: "DFARS 252.204-7012 requires NIST SP 800-171 safeguarding and rapid incident reporting. GCC High is the environment engineered to meet the clause you already signed.",
  },
  {
    tag: "CMMC L2",
    title: "The assessment that's coming",
    body: "CMMC is phasing into DoD solicitations now. Level 2 means a third-party assessment against 800-171 — and an enclave built for those controls is how you pass it the first time.",
  },
];

// The managed service — the four bands, with what each includes.
const deliver = [
  {
    idx: "01",
    accent: "var(--sunset)",
    title: "Design & build the enclave",
    lede: "A compliant Microsoft 365 GCC High tenant, hardened to a CUI-ready baseline from day one.",
    points: [
      "GCC High tenant provisioning and hardening",
      "Entra ID, Conditional Access, MFA, device compliance",
      "Microsoft Defender and Purview (DLP, sensitivity labels, audit)",
      "Baseline mapped to NIST SP 800-171 from the start",
    ],
  },
  {
    idx: "02",
    accent: "var(--steel)",
    title: "Migrate without spillage",
    lede: "Your people and data moved into the boundary cleanly — no CUI left where it shouldn't be.",
    points: [
      "Email, OneDrive, SharePoint, Teams, and identities",
      "From Commercial Microsoft 365 or GCC into GCC High",
      "Data classification and spillage controls in flight",
      "Coexistence during cutover, minimal downtime",
    ],
  },
  {
    idx: "03",
    accent: "var(--coral)",
    title: "Engineer & document the controls",
    lede: "The 800-171 control set implemented, documented, and ready to be assessed.",
    points: [
      "All 110 NIST SP 800-171 controls implemented",
      "System Security Plan (SSP) authored and maintained",
      "POA&M for any residual gaps, tracked to closure",
      "Evidence organized for a C3PAO Level 2 assessment",
    ],
  },
  {
    idx: "04",
    accent: "var(--seafoam)",
    title: "Manage & sustain",
    lede: "The ongoing operations that keep the enclave compliant long after go-live.",
    points: [
      "Day-to-day administration and user lifecycle",
      "Patching, monitoring, and configuration-drift control",
      "Security operations and incident-response support",
      "Help desk for your cleared users",
    ],
  },
];

// How the engagement is delivered. The distinction that matters to a buyer
// comparing us to a hosted-enclave vendor: the tenant is theirs, not ours.
const model = [
  {
    idx: "01",
    accent: "var(--sunset)",
    title: "You own the tenant",
    body: "The GCC High subscription is registered to your company. Entra ID, the data, the keys, the audit logs — all yours. If you replace us, you keep the environment and everything in it. There is nothing of ours to migrate out of.",
  },
  {
    idx: "02",
    accent: "var(--steel)",
    title: "We run the operations",
    body: "Identity and device policy, patching, monitoring, configuration-drift control, security operations, incident response, and help desk for your cleared users — inside your boundary, on your side of the line.",
  },
  {
    idx: "03",
    accent: "var(--seafoam)",
    title: "Licensed to your actual CUI footprint",
    body: "Only the people who touch CUI need a GCC High seat. Engineering and contracts move in; the rest of the business can stay on commercial Microsoft 365 with the boundary enforced between them. Scoping that footprint is where most of the cost control lives.",
  },
];

// The three lines a managed GCC High engagement actually costs money on.
// Figures stay as {{TOKEN}} placeholders until confirmed — `pending()` keeps an
// unfilled one from rendering, so the card shows its scope without a number.
const pricing = [
  {
    idx: "01",
    accent: "var(--sunset)",
    title: "Build & migration",
    body: "A fixed-scope project: tenant provisioning, the CUI-ready hardening baseline, the 800-171 control implementation, and the migration itself. Priced on user count, data volume, and what you are moving from — Commercial, GCC, or nothing yet.",
    figure: BUILD_PROJECT_RANGE,
    unit: "typical engagement",
  },
  {
    idx: "02",
    accent: "var(--steel)",
    title: "Managed operations",
    body: "The monthly fee for running it: administration, patching, monitoring, drift control, security operations, help desk, and keeping your SSP and evidence current between assessments.",
    figure: MANAGED_STARTING_PRICE,
    unit: "per user / month, starting at",
  },
  {
    idx: "03",
    accent: "var(--seafoam)",
    title: "Microsoft licensing",
    body: "GCC High G3 or G5, per user per month, on Microsoft's price list. We source and provision it so there is no separate reseller to chase — and only the seats inside your CUI footprint need it.",
    figure: pending(G5_LIST_PRICE) ? G5_LIST_PRICE : `${G3_LIST_PRICE} G3 · ${G5_LIST_PRICE} G5`,
    unit: "per user / month, list",
  },
];

// The 14 NIST SP 800-171 control families CMMC Level 2 assesses.
const families = [
  "Access Control", "Awareness & Training", "Audit & Accountability",
  "Configuration Management", "Identification & Authentication", "Incident Response",
  "Maintenance", "Media Protection", "Personnel Security", "Physical Protection",
  "Risk Assessment", "Security Assessment", "System & Communications Protection",
  "System & Information Integrity",
];

const faqs = [
  {
    q: "What is Microsoft 365 GCC High?",
    a: "Microsoft 365 GCC High is a dedicated, US-sovereign deployment of Microsoft 365 that runs in Microsoft's Azure Government cloud. It is physically and logically separated from the commercial and standard-government (GCC) environments, keeps data in the continental US, and is supported only by screened US persons. It exists so defense contractors can handle CUI, ITAR, and export-controlled data under DFARS and CMMC.",
  },
  {
    q: "What's the difference between GCC and GCC High?",
    a: "GCC (Government Community Cloud) runs on commercial infrastructure with some government features and is fine for many state, local, and federal-civilian needs. GCC High runs in Azure Government with stricter personnel screening, US-person support, and data-residency controls, and is the environment recommended — and often required — for CUI and ITAR-regulated defense data. If ITAR or DFARS/CMMC is in your contract, GCC High is usually the answer.",
  },
  {
    q: "Do I actually need GCC High?",
    a: "If your DoD contracts flow down DFARS 252.204-7012, involve CUI, or touch ITAR/export-controlled technical data, you almost certainly need GCC High. If you're unsure, we'll review your contract clauses and data types and tell you honestly — sometimes GCC or Commercial with the right controls is enough, and we'll say so rather than sell you a bigger environment than you need.",
  },
  {
    q: "Does moving to GCC High make me CMMC compliant?",
    a: "No environment makes you compliant on its own. CMMC Level 2 is a third-party assessment against the 110 NIST SP 800-171 controls. What GCC High gives you is a foundation built for those controls. Optimal implements and documents the control set inside your enclave (SSP and POA&M), collects the evidence, and gets you assessment-ready — but the certified assessment is performed independently by an accredited C3PAO. We prepare the environment; we don't grade our own work.",
  },
  {
    q: "How long does a GCC High migration take?",
    a: "Most migrations run four to eight weeks, depending on user count, the volume and sensitivity of the data, and your source environment. We scope it precisely up front and run the cutover with coexistence so your team keeps working through the transition.",
  },
  {
    q: "Can I use Copilot or AI inside GCC High?",
    a: "Yes, increasingly. Azure OpenAI is available in Azure Government today, and Microsoft 365 Copilot is rolling out to GCC High. Optimal stands up governed access to whatever AI is authorized for your environment — behind an authenticated gateway with fail-closed guardrails and an audit trail — so your cleared users get AI on mission work without data leaving the boundary. That governed-AI layer is what sets us apart from a migration-only shop.",
  },
  {
    q: "Is this a hosted enclave?",
    a: "No. Some vendors sell a hosted enclave — their infrastructure, their tenant, your CUI living inside a boundary they own. Optimal stands up a Microsoft 365 GCC High tenant registered to your company and manages it for you. The distinction matters when things change: a tenant you own scales with your headcount, holds your data under your keys and your audit logs, and stays yours if you replace us. With a hosted enclave, leaving the provider means moving your CUI out of someone else's environment — and losing access to it is their decision, not yours.",
  },
  {
    q: "Do I need GCC High licenses for everyone in my company?",
    a: "Usually not. Only the users who actually handle CUI or export-controlled technical data need to be inside the GCC High boundary. In most contractors that is engineering, contracts, and program management — not the whole company. The rest can stay on commercial Microsoft 365 with the boundary enforced between the two. We scope your CUI footprint first and license to it, because that is where most of the cost is decided.",
  },
  {
    q: "What does a managed GCC High enclave cost?",
    a: "It prices as three lines. First, a fixed-scope build and migration project, driven by user count, data volume, and what you are migrating from. Second, a monthly per-user fee for managed operations — administration, patching, monitoring, security operations, help desk, and keeping your SSP and evidence current. Third, Microsoft licensing, which is a per-seat cost on Microsoft's price list. We scope the CUI footprint and quote all three before you sign, and if a smaller scope covers your obligations we will tell you that instead.",
  },
  {
    q: "What does GCC High licensing cost per user?",
    a: "GCC High licensing is per user per month, on Microsoft's published price list, and the tier you need — G3 or G5 — depends on which security and compliance capabilities your control set relies on. Microsoft raised GCC High list pricing in July 2026, roughly 8% on G3 and 5% on G5, so budgets written before that need revisiting. Licensing is the line that moves without us; scoping the CUI footprint tightly is the control you have over it. We source and provision the licenses as part of the engagement.",
  },
  {
    q: "How does Optimal work as an External Service Provider (ESP)?",
    a: "Bringing in a managed provider does not move your obligation — it splits it. Under 32 CFR 170 the relationship has to be documented in your System Security Plan, and an assessor will expect a Customer Responsibility Matrix showing which controls the provider operates and which stay with you. Every managed engagement ships with a Shared Responsibility Matrix covering all 110 NIST SP 800-171 controls, each marked Optimal-owned, client-owned, or shared, with the evidence artifact that proves it — maintained as the environment changes rather than written once at go-live. Optimal prepares the environment and the evidence; the certified assessment is performed independently by an accredited C3PAO.",
  },
  {
    q: "Do you provide the GCC High licenses?",
    a: "Yes. We source and provision your Microsoft 365 GCC High G5 licensing and stand up the tenant, so you don't have to manage a separate licensing relationship on top of everything else.",
  },
  {
    q: "Do you manage the environment after migration?",
    a: "Yes — that's the point. After go-live we run day-to-day administration, patching, monitoring, user lifecycle, security operations, and help desk, and we keep your SSP and evidence current so you stay assessment-ready between contracts and re-assessments.",
  },
];

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((f) => ({
    "@type": "Question",
    name: f.q,
    acceptedAnswer: { "@type": "Answer", text: f.a },
  })),
};

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Managed Microsoft 365 GCC High Enclave",
  serviceType: "Managed GCC High enclave for CMMC compliance",
  provider: { "@type": "Organization", name: "Optimal", url: "https://gooptimal.io" },
  areaServed: "United States defense industrial base",
  audience: {
    "@type": "Audience",
    audienceType: "Defense contractors handling CUI and ITAR-controlled data",
  },
  description:
    "Optimal designs, builds, migrates, and manages CMMC-ready Microsoft 365 GCC High enclaves for the defense industrial base on a tenant the client owns — aligned to DFARS 252.204-7012, NIST SP 800-171, and CMMC Level 2, with support for CUI and ITAR-controlled data, scoped GCC High G3/G5 licensing, a Shared Responsibility Matrix documenting the External Service Provider relationship under 32 CFR 170, and governed AI inside the boundary.",
};
---

<Layout
  title="Managed Microsoft 365 GCC High — cost, CMMC compliance & shared responsibility | Optimal"
  description="Optimal builds, migrates, and manages CMMC-ready Microsoft 365 GCC High enclaves for the defense industrial base — on a tenant you own, not a hosted enclave we own. Aligned to DFARS 252.204-7012, NIST SP 800-171, and CMMC Level 2, with CUI and ITAR support, scoped GCC High G3/G5 licensing, a Shared Responsibility Matrix for your assessor, and governed AI inside the boundary. An SDVOSB."
  keywords="GCC High, Microsoft 365 GCC High, managed GCC High, GCC High migration, GCC High MSP, CMMC, CMMC Level 2, CMMC compliance, NIST SP 800-171, DFARS 252.204-7012, CUI, Controlled Unclassified Information, ITAR compliant cloud, defense industrial base, DIB, DoD contractor, CUI enclave, GCC High cost, GCC High pricing, GCC High licensing cost, managed GCC High pricing, GCC High G3, GCC High G5, External Service Provider, ESP, 32 CFR 170, shared responsibility matrix, customer responsibility matrix, Azure Government, SDVOSB"
  breadcrumbs={[{ name: "Home", href: "/" }]}
  jsonLd={[serviceSchema, faqSchema]}
>
  <Header />

  <!-- HERO -->
  <section class="hero relative">
    <div class="glow"></div>
    <GatewayCanvas variant="hero" />
    <div class="container z">
      <span class="eyebrow">Managed Microsoft 365 GCC High · For the defense industrial base</span>
      <h1 class="display">Your CMMC-ready enclave —<br /><span class="grad">built, migrated, and managed.</span></h1>
      <p class="lead">
        Handling CUI or ITAR-controlled data on a DoD contract means meeting DFARS 252.204-7012 and,
        increasingly, CMMC Level 2. Optimal stands up your Microsoft&nbsp;365 GCC&nbsp;High environment,
        migrates your people and data into it, engineers the NIST SP 800-171 controls, and runs it for
        you — so compliance becomes a capability you operate, not a fire drill before every award.
      </p>
      <p class="hero-caps">
        Build <span>/</span> Migrate <span>/</span> Engineer controls <span>/</span> Manage
      </p>
      <div class="cta-row">
        <a href="/contact" class="btn btn-primary">Book a scoping call →</a>
        <a href="#deliver" class="btn btn-ghost">See what's included</a>
      </div>
      <p class="cta-aside muted">
        Not sure yet? <a href="/do-i-need-gcc-high">Do I actually need GCC High? Get the 2-minute answer →</a>
      </p>
      <div class="hero-meta muted">
        <span>SDVOSB-certified</span><span class="dot">•</span>
        <span>CUI &amp; ITAR-ready</span><span class="dot">•</span>
        <span>GCC High G5 licensing included</span>
      </div>
    </div>
  </section>

  <!-- WHY GCC HIGH -->
  <section id="why" class="section--tight why relative">
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">Why GCC High</span>
        <h2 class="h2">Commercial Microsoft 365 wasn't built for defense data. GCC High was.</h2>
        <p class="lead" style="margin-top:14px;">Four things drive a defense contractor into GCC High. If any of them are in your contracts, the environment isn't optional.</p>
      </div>
      <div class="driver-grid">
        {drivers.map((d) => (
          <article class="card driver">
            <span class="tag">{d.tag}</span>
            <h3>{d.title}</h3>
            <p class="muted">{d.body}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- DELIVERY MODEL -->
  <section id="model" class="section--tight model relative">
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">The delivery model</span>
        <h2 class="h2">Your tenant. Your data. Our management.</h2>
        <p class="lead" style="margin-top:14px;">Some vendors sell a hosted enclave — their infrastructure, their tenant, your CUI living inside a boundary they own. We don't. Optimal stands up a GCC High tenant registered to your company and runs it for you.</p>
      </div>
      <div class="model-grid">
        {model.map((m) => (
          <article class="card mband">
            <span class="mband-ix" style={`color:${m.accent}`}>{m.idx}</span>
            <h3>{m.title}</h3>
            <p class="muted">{m.body}</p>
          </article>
        ))}
      </div>
      <p class="muted note">
        <strong>How we scope it:</strong> we review your contracts and your CUI footprint and tell you what
        you actually need — including when a smaller scope is enough. We won't quote a company-wide
        migration for a problem that lives in one department.
      </p>
    </div>
  </section>

  <!-- WHAT WE DELIVER -->
  <section id="deliver" class="section deliver relative">
    <div class="glow"></div>
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">The managed service</span>
        <h2 class="h2">One partner, from first tenant to steady state.</h2>
        <p class="lead" style="margin-top:14px;">Other shops migrate your email and hand you the keys. Optimal builds the enclave, moves you in, engineers the controls, and keeps running it — the whole lifecycle, on one contract.</p>
      </div>
      <div class="deliver-grid">
        {deliver.map((b) => (
          <article class="card dband">
            <header class="dband-top">
              <span class="dband-ix" style={`color:${b.accent}`}>{b.idx}</span>
              <h3>{b.title}</h3>
            </header>
            <p class="muted dband-lede">{b.lede}</p>
            <ul class="dband-points">
              {b.points.map((pt) => <li>{pt}</li>)}
            </ul>
          </article>
        ))}
      </div>
      <p class="muted note">GCC High G5 licensing and provisioning included — we source and stand up your licenses and tenant, so there's no separate reseller to chase.</p>
    </div>
  </section>

  <!-- PRICING -->
  <section id="pricing" class="section--tight pricing relative">
    <div class="glow"></div>
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">What it costs</span>
        <h2 class="h2">Three lines, and we name all three before you sign.</h2>
        <p class="lead" style="margin-top:14px;">Nobody should have to reverse-engineer a GCC High quote. A managed environment costs money in three places — the project that builds it, the operations that run it, and the Microsoft licensing underneath both.</p>
      </div>
      <div class="price-grid">
        {pricing.map((c) => (
          <article class="card pcard">
            <span class="pcard-ix" style={`color:${c.accent}`}>{c.idx}</span>
            <h3>{c.title}</h3>
            {!pending(c.figure) && (
              <p class="pfig">
                <span class="pfig-num" style={`color:${c.accent}`}>{c.figure}</span>
                <span class="pfig-unit">{c.unit}</span>
              </p>
            )}
            <p class="muted">{c.body}</p>
          </article>
        ))}
      </div>
      <p class="muted note">
        Microsoft raised GCC High list pricing in July 2026 — roughly 8% on G3 and 5% on G5 — so a budget
        written before that needs revisiting. Licensing is the line that moves without us; scoping the CUI
        footprint tightly is the control you have over it. Ask for the numbers on the scoping call and
        we'll quote all three against your actual user count.
      </p>
    </div>
  </section>

  <!-- CMMC / 800-171 ALIGNMENT -->
  <section id="cmmc" class="section--tight cmmc relative">
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">CMMC Level 2 · NIST SP 800-171</span>
        <h2 class="h2">Built for the 110 controls — and the assessment that checks them.</h2>
        <p class="lead" style="margin-top:14px;">CMMC Level 2 assesses your program against the 14 families and 110 controls of NIST SP 800-171. We implement them in the enclave, document them in your SSP, track residual gaps in a POA&amp;M, and hand your assessor an environment that's ready to be graded.</p>
      </div>
      <div class="families">
        {families.map((f) => <span class="chip">{f}</span>)}
      </div>
      <p class="muted boundary">
        <strong>Where we draw the line:</strong> Optimal builds and prepares the environment and gets you
        assessment-ready. The certified CMMC assessment is performed independently by an accredited C3PAO —
        we don't assess our own work, and we won't tell you you're compliant. We'll tell you you're ready.
      </p>
    </div>
  </section>

  <!-- ESP / SHARED RESPONSIBILITY -->
  <section id="esp" class="section esp relative">
    <div class="glow"></div>
    <div class="container z sec-split">
      <div class="sec-head">
        <span class="ix">External Service Provider</span>
        <h2 class="h2">Your assessor will ask who owns which control.</h2>
      </div>
      <div class="edge-body">
        <p>
          Bringing in a managed provider doesn't move your obligation — it splits it. Under 32 CFR 170 the
          relationship has to be documented in your System Security Plan, and an assessor will expect a
          Customer Responsibility Matrix showing which controls the provider operates and which stay with you.
          An ESP that can't produce one is a finding waiting to happen.
        </p>
        <p>
          Every managed engagement ships with a <strong>Shared Responsibility Matrix</strong>: all 110 NIST
          SP 800-171 controls, each marked Optimal-owned, client-owned, or shared, with the evidence artifact
          that proves it. It's maintained as the environment changes — not written once at go-live and left
          to drift out of date before your assessment.
        </p>
        {!pending(ESP_POSTURE) && <p>{ESP_POSTURE}</p>}
        <p>
          The matrix is written for your assessor, not for us. Optimal engineers the environment and prepares
          the evidence; the certified CMMC assessment is performed independently by an accredited C3PAO. We
          prepare, we don't grade.
        </p>
        {!pending(RPO_STATUS) && (
          <p class="rpo"><span class="rpo-k">Cyber AB</span>{RPO_STATUS}</p>
        )}
      </div>
    </div>
  </section>

  <!-- VULNERABILITY MANAGEMENT & EVIDENCE ENGINE -->
  <section id="evidence" class="section--tight evidence relative">
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">Included in the managed service</span>
        <h2 class="h2">Vulnerability management that produces evidence, not a backlog.</h2>
        <p class="lead" style="margin-top:14px;">
          800-171 expects you to inventory what you run, find the flaws in it, and show that you
          remediated the ones that mattered. Most contractors answer that with a scanner report nobody
          can act on. Your enclave runs NINELINE — Optimal&rsquo;s reference implementation of AI-native
          supply-chain security, deployed inside your boundary.
        </p>
      </div>
      <ul class="ev-points">
        <li>
          <h3>Continuous SBOM</h3>
          <p class="muted">An authoritative bill of materials for the software you build and run, differenced on every change, with new dependencies narrated in mission terms as they land.</p>
        </li>
        <li>
          <h3>Reachability-ranked findings</h3>
          <p class="muted">A frontier model reads the actual codebase — grepping, opening files, tracing call chains — to decide whether a vulnerable path is reachable from an entry point. In the reference scan that took 874 raw findings down to the 18 that required action.</p>
        </li>
        <li>
          <h3>CISA KEV &amp; EPSS enrichment</h3>
          <p class="muted">Known-exploited status and exploitation probability fused with the reachability verdict, so the queue is ordered by what an adversary can actually use — not by CVSS score.</p>
        </li>
        <li>
          <h3>Assessment-ready evidence</h3>
          <p class="muted">Asset inventory, flaw remediation, and risk-assessment artifacts your assessor can read directly — and the continuous-monitoring posture a continuous-ATO program expects.</p>
        </li>
      </ul>
      <p class="muted note">
        <strong>It runs inside the boundary</strong> — your tenant, your infrastructure, air-gap capable.
        That is the difference between this and a SaaS scanner: the bill of materials for a defense
        program&rsquo;s codebase is itself sensitive, and most tooling that analyzes it requires you to ship
        it to somebody else&rsquo;s cloud. A migration-only MSP doesn&rsquo;t offer this at all.
        <a class="inline-link" href="/nineline">See how NINELINE works →</a>
      </p>
    </div>
  </section>

  <!-- THE AI EDGE -->
  <section id="ai-edge" class="section ai-edge relative">
    <div class="glow"></div>
    <div class="container z sec-split">
      <div class="sec-head">
        <span class="ix">The difference</span>
        <h2 class="h2">The GCC High partner that also gives you AI.</h2>
      </div>
      <div class="edge-body">
        <p>
          Most GCC High shops migrate your mailboxes and disappear. Optimal's other half is AI security —
          so once the enclave is standing, we stand up governed access to the frontier models authorized
          for your sovereign cloud: Azure OpenAI in Azure Government, and Microsoft 365 Copilot as it
          reaches GCC High.
        </p>
        <p>
          One authenticated gateway, fail-closed guardrails, default-deny egress, and an audit trail — the
          same open-source Zero Trust pattern we publish. Your cleared teams get real AI on mission work,
          governed and logged, inside the boundary.
          <a class="inline-link" href="/#how">See the architecture →</a>
        </p>
      </div>
    </div>
  </section>

  <!-- WHO IT'S FOR -->
  <section id="who" class="section--tight who relative">
    <div class="container z who-inner">
      <div class="who-copy">
        <span class="ix">Who it's for</span>
        <h2 class="h2">Built for the defense industrial base.</h2>
        <p class="lead" style="margin-top:14px;">
          Primes and subcontractors, manufacturers, aerospace and defense, and R&amp;D shops — anyone with
          CUI or ITAR obligations flowing down from a prime. Especially the small and mid-size contractors
          who can't staff a full compliance and IT security team, and can't afford to lose an award over it.
        </p>
      </div>
      <div class="vet-card">
        <VetBadge size={72} />
        <p class="muted">
          Optimal, LLC is an SBA-certified <strong>Service-Disabled Veteran-Owned Small Business</strong> —
          teaming- and set-aside-friendly for your subcontracting goals.
        </p>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section id="faq" class="section faq relative">
    <div class="glow"></div>
    <div class="container z">
      <div class="sec-head wide">
        <span class="ix">FAQ</span>
        <h2 class="h2">GCC High &amp; CMMC, answered.</h2>
      </div>
      <div class="faq-list">
        {faqs.map((f) => (
          <details class="faq-item">
            <summary>
              <span class="faq-q">{f.q}</span>
              <span class="faq-mark" aria-hidden="true"></span>
            </summary>
            <p class="muted faq-a">{f.a}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section cta relative">
    <div class="glow"></div>
    <GatewayCanvas variant="ambient" align="center" density={0.7} />
    <div class="container z cta-inner">
      <h2 class="display" style="font-size:clamp(2rem,5vw,3.4rem);">Let's stand up<br />your enclave.</h2>
      <p class="lead" style="margin:18px auto 0;">Tell us the contracts you're chasing, the data you handle, and where you are today — Commercial, GCC, or nothing yet. We'll scope the migration, the controls, and the managed service, and give you a straight answer on GCC High.</p>
      <a href="/contact" class="btn btn-primary" style="margin-top:28px;">Book a scoping call →</a>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  .ix {
    display: inline-block; font-family: var(--font-mono); font-size: 12px;
    letter-spacing: 0.14em; text-transform: uppercase; color: var(--text-faint);
    margin-bottom: 16px;
  }
  .sec-head.wide { max-width: 64ch; }
  .inline-link { color: var(--sunset); font-weight: 600; }
  .inline-link:hover { color: var(--ember); }
  .note { margin-top: 26px; font-size: 14px; max-width: 72ch; }

  /* HERO */
  .hero { padding: clamp(72px, 12vw, 140px) 0 clamp(48px, 7vw, 84px); }
  .hero .eyebrow { margin-bottom: 22px; }
  .hero h1 { margin-bottom: 24px; }
  .hero .lead { font-size: clamp(17px, 2.2vw, 21px); }
  .hero-caps {
    margin-top: 22px; font-family: var(--font-mono); font-size: 12.5px;
    letter-spacing: 0.06em; color: var(--text-dim); text-transform: uppercase;
  }
  .hero-caps span { color: var(--text-faint); margin: 0 4px; }
  .cta-row { display: flex; flex-wrap: wrap; gap: 14px; margin-top: 30px; }
  .hero-meta { display: flex; flex-wrap: wrap; gap: 12px; align-items: center; margin-top: 30px; font-size: 13px; }
  .cta-aside { margin-top: 18px; font-size: 13.5px; }
  .cta-aside a { color: var(--sunset); font-weight: 600; }
  .cta-aside a:hover { color: var(--ember); }
  .hero-meta .dot { opacity: 0.4; }

  /* WHY */
  .why { background: var(--surface); border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .driver-grid { display: grid; gap: 18px; margin-top: 40px; }
  @media (min-width: 680px) { .driver-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (min-width: 1000px) { .driver-grid { grid-template-columns: repeat(4, 1fr); } }
  .driver h3 { font-size: 1.15rem; margin: 12px 0 10px; }
  .driver p { font-size: 13.5px; line-height: 1.65; }

  /* DELIVER */
  .deliver { background: var(--surface); border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .deliver-grid { display: grid; gap: 20px; margin-top: 42px; }
  @media (min-width: 800px) { .deliver-grid { grid-template-columns: repeat(2, 1fr); } }
  .dband { display: flex; flex-direction: column; }
  .dband-top { display: flex; align-items: baseline; gap: 12px; margin-bottom: 12px; }
  .dband-ix { font-family: var(--font-mono); font-size: 13px; font-weight: 600; letter-spacing: 0.05em; }
  .dband h3 { font-size: 1.25rem; }
  .dband-lede { font-size: 14.5px; line-height: 1.6; }
  .dband-points {
    list-style: none; margin: 16px 0 0; padding: 16px 0 0;
    border-top: 1px solid var(--rule); display: grid; gap: 8px;
  }
  .dband-points li {
    position: relative; padding-left: 18px; font-size: 13.5px; color: var(--text-dim); line-height: 1.5;
  }
  .dband-points li::before {
    content: ""; position: absolute; left: 2px; top: 8px; width: 5px; height: 5px;
    border-radius: 1px; background: var(--rule-strong); transform: rotate(45deg);
  }

  /* DELIVERY MODEL */
  .model { background: var(--bg); }
  .model-grid { display: grid; gap: 18px; margin-top: 40px; }
  @media (min-width: 900px) { .model-grid { grid-template-columns: repeat(3, 1fr); } }
  .mband-ix { font-family: var(--font-mono); font-size: 13px; font-weight: 600; letter-spacing: 0.05em; }
  .mband h3 { font-size: 1.2rem; margin: 12px 0 10px; }
  .mband p { font-size: 13.5px; line-height: 1.65; }
  .note strong { color: var(--text); }

  /* PRICING */
  .pricing { background: var(--bg); }
  .price-grid { display: grid; gap: 18px; margin-top: 40px; }
  @media (min-width: 900px) { .price-grid { grid-template-columns: repeat(3, 1fr); } }
  .pcard-ix { font-family: var(--font-mono); font-size: 13px; font-weight: 600; letter-spacing: 0.05em; }
  .pcard h3 { font-size: 1.2rem; margin: 12px 0 10px; }
  .pcard p.muted { font-size: 13.5px; line-height: 1.65; }
  .pfig {
    display: flex; flex-direction: column; gap: 5px;
    margin: 0 0 14px; padding-bottom: 14px; border-bottom: 1px solid var(--rule);
  }
  .pfig-num { font-family: var(--font-display); font-weight: 700; font-size: 1.6rem; letter-spacing: -0.01em; }
  .pfig-unit { font-family: var(--font-mono); font-size: 11px; text-transform: uppercase; letter-spacing: 0.08em; color: var(--text-dim); }

  /* ESP / SHARED RESPONSIBILITY */
  .esp { background: var(--bg); }
  .esp .edge-body strong { color: var(--text); }
  .rpo { font-family: var(--font-mono); font-size: 13px; color: var(--text-dim) !important; }
  .rpo-k {
    display: inline-block; margin-right: 10px; font-size: 11px;
    text-transform: uppercase; letter-spacing: 0.14em; color: var(--text-faint);
  }

  /* CMMC */
  .cmmc { background: var(--surface); border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .families { display: flex; flex-wrap: wrap; gap: 10px; margin-top: 34px; }
  .chip {
    font-family: var(--font-mono); font-weight: 500; font-size: 12.5px; letter-spacing: 0.02em;
    color: var(--text-dim); border: 1px solid var(--rule); border-radius: 7px;
    padding: 7px 13px; transition: color .2s, border-color .2s;
  }
  .chip:hover { color: var(--text); border-color: var(--rule-strong); }
  .boundary { margin-top: 28px; font-size: 14.5px; line-height: 1.7; max-width: 74ch; }
  .boundary strong { color: var(--text); }

  /* EVIDENCE ENGINE */
  .evidence { background: var(--surface); border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .ev-points { list-style: none; margin: 38px 0 0; padding: 0; display: grid; gap: 20px; }
  @media (min-width: 760px) { .ev-points { grid-template-columns: repeat(2, 1fr); gap: 24px 40px; } }
  .ev-points li { padding-top: 18px; border-top: 1px solid var(--rule); }
  .ev-points h3 { font-size: 1.05rem; margin-bottom: 9px; }
  .ev-points p { font-size: 13.5px; line-height: 1.65; }

  /* AI EDGE */
  .ai-edge { background: var(--bg); }
  .sec-split { display: grid; gap: 26px; }
  .edge-body { display: grid; gap: 18px; max-width: 64ch; }
  .edge-body p { color: var(--text-dim); font-size: clamp(15px, 1.8vw, 17px); line-height: 1.75; }
  @media (min-width: 900px) {
    .sec-split { grid-template-columns: 0.85fr 1.15fr; gap: 56px; align-items: start; }
  }

  /* WHO */
  .who { background: var(--surface); border-top: 1px solid var(--rule); border-bottom: 1px solid var(--rule); }
  .who-inner { display: grid; gap: 30px; align-items: center; }
  .who-copy { max-width: 62ch; }
  .vet-card {
    display: flex; flex-direction: column; gap: 14px; align-items: flex-start;
    padding: 26px; border: 1px solid var(--rule); border-radius: var(--radius);
    background: linear-gradient(180deg, var(--surface-2), var(--surface));
  }
  .vet-card p { font-size: 13.5px; line-height: 1.6; }
  .vet-card strong { color: var(--text); }
  @media (min-width: 900px) { .who-inner { grid-template-columns: 1.4fr 1fr; gap: 48px; } }

  /* FAQ */
  .faq { background: var(--bg); }
  .faq-list { margin-top: 34px; max-width: 82ch; border-top: 1px solid var(--rule); }
  .faq-item { border-bottom: 1px solid var(--rule); }
  .faq-item summary {
    display: flex; align-items: center; justify-content: space-between; gap: 18px;
    padding: 20px 4px; cursor: pointer; list-style: none;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-q { font-family: var(--font-display); font-weight: 600; font-size: clamp(1rem, 2vw, 1.15rem); color: var(--text); letter-spacing: -0.01em; }
  .faq-mark { position: relative; flex: 0 0 auto; width: 14px; height: 14px; }
  .faq-mark::before, .faq-mark::after {
    content: ""; position: absolute; background: var(--sunset); transition: transform .2s ease, opacity .2s ease;
  }
  .faq-mark::before { top: 6px; left: 0; width: 14px; height: 2px; }
  .faq-mark::after { top: 0; left: 6px; width: 2px; height: 14px; }
  .faq-item[open] .faq-mark::after { transform: scaleY(0); opacity: 0; }
  .faq-a { padding: 0 4px 22px; font-size: 15px; line-height: 1.75; max-width: 74ch; }

  /* CTA */
  .cta { background: var(--surface); border-top: 1px solid var(--rule); }
  .cta-inner { text-align: center; max-width: 720px; margin: 0 auto; }
</style>
